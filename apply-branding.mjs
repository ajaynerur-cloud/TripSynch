import fs from "node:fs";
for(const folder of ["frontend","docs"]){const hp=`${folder}/index.html`,cp=`${folder}/styles.css`;if(!fs.existsSync(hp)||!fs.existsSync(cp))continue;let h=fs.readFileSync(hp,"utf8");if(!h.includes('rel="icon"'))h=h.replace('<link rel="manifest" href="manifest.webmanifest">','<link rel="manifest" href="manifest.webmanifest"><link rel="icon" href="favicon.svg" type="image/svg+xml">');h=h.replace(/<div class="logo">.*?<\/div>/s,'<img class="welcome-logo" src="assets/logo.svg" alt="TripSynch logo">');if(!h.includes('class="app-logo"'))h=h.replace('<header class="appbar"><div>','<header class="appbar"><div class="appbar-brand"><img class="app-logo" src="assets/logo.svg" alt=""><div>').replace('<p id="sync"></p></div><button id="invite">','<p id="sync"></p></div></div><button id="invite">');fs.writeFileSync(hp,h);let c=fs.readFileSync(cp,"utf8");if(!c.includes('.welcome-logo{'))c+='\n.welcome-logo{width:76px;height:76px;border-radius:22px;box-shadow:0 16px 36px #075e5530}.appbar-brand{display:flex;align-items:center;gap:11px}.app-logo{width:44px;height:44px;border-radius:13px;background:#fff}.appbar-brand>div{min-width:0}\n';fs.writeFileSync(cp,c)}
if(fs.existsSync("package.json")){const p=JSON.parse(fs.readFileSync("package.json","utf8"));p.devDependencies={...(p.devDependencies||{}),"@capacitor/assets":"^3.0.5"};p.scripts={...(p.scripts||{}),"assets:generate":"npx capacitor-assets generate"};fs.writeFileSync("package.json",JSON.stringify(p,null,2)+"\n")}console.log("TripSynch branding applied.");